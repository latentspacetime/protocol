# Colors are owned by the selected theme. Keeping color values out of this
# file allows the theme picker to switch the complete palette atomically.
theme = 12-bit Rainbow

cursor-style = block
cursor-style-blink = true
